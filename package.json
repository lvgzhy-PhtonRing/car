{
  "name": "car-accounting",
  "version": "1.1.1",
  "private": true,
  "description": "记账管理系统 - 热更新开发服务器",
  "scripts": {
    "dev": "browser-sync start --server . --index V1.1.html --files \"*.html,*.json,*.css,*.js\" --no-notify --no-ghost-mode --port 3000",
    "dev:reload": "browser-sync start --server . --index V1.1.html --files \"*.html,*.json\" --no-notify --port 3000"
  },
  "devDependencies": {
    "browser-sync": "^3.0.3"
  }
}
